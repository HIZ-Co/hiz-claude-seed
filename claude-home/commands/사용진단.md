---
description: 최근 14일 Claude Code 사용 패턴을 7개 축으로 자가 진단하고 c:/team-claude-review/에 저장
---

`C:\team-claude-review\_analyze.py`를 실행해 `_analysis.json`을 생성한 뒤,
거기 통계를 근거로 사용자의 Claude Code 사용 패턴을 평가하세요.

## 입력
- 데이터: `C:\Users\green\.claude\projects\` 하위 .jsonl (최근 14일)
- **subagents/ 폴더는 제외** (AI가 디스패치한 작업이라 사용자 평가에 섞이면 안 됨)

## 평가 축 (각 1~5점, 총 35점)

1. **프롬프트 명확성** — 맥락·목표·제약 명시도
2. **툴 활용도** — Read/Grep/Glob/Edit 적절성, 병렬 호출 여부
3. **슬래시 커맨드·스킬 활용** — /로 시작하는 기능 활용 빈도
4. **검증 습관** — "됐다" 전에 직접 확인하는 비율
5. **컨텍스트 관리** — CLAUDE.md, memory, plan mode 사용
6. **실패 복구** — 잘못된 결과를 받았을 때 재지시 품질
7. **반복 학습** — 같은 실수를 두 번 안 하는지

각 축마다:
- **근거 인용**: `_analysis.json`의 수치 + 실제 트랜스크립트 1줄 인용 (세션 ID 포함)
- **판단**: 2~3문장, 사실 기반·구체적
- **개선 액션**: 다음 주 실행할 3개 (체크박스)

## 톤 규칙

- 형식적 칭찬 금지 ("잘하고 있어요" 류 표현 금지)
- 평균 3/5가 기본. **4점부터는 명확한 근거** 필요
- 점수는 보수적으로. 인플레 금지

## 출력

`C:\team-claude-review\{이름}-{YYYY-MM-DD}.md`로 저장.
- 이름은 사용자에게 묻거나 git config user.name 사용
- 같은 날짜에 기존 파일 있으면 `-v2`, `-v3` 붙이기

## 완료 후 보고

저장 경로 + 총점 + 강점 1개 + 약점 1개 + 다음 주 P0 액션 1개만 인라인으로 짧게.
